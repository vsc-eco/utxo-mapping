.DS_Store
*.wasm
# Mock router test fixture (see BTC-C4 refund path test). Built once
# from tests/mocks/mockrouter/main.go and checked into git so test runs
# don't depend on tinygo being installed.
!**/tests/mocks/mockrouter/bin/mock_router.wasm
# Draining mock router (VR2-23 regression): pulls its allowance BEFORE reporting
# zero output, so the refund path is exercised adversarially, not just benignly.
!**/tests/mocks/mockdrainrouter/bin/mock_drain_router.wasm
.env
data/
go.work
go.work.sum
.claude/
.vscode/